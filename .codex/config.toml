# Project defaults for active work in this repository.
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[agents]
max_threads = 6
max_depth = 1
interrupt_message = true
